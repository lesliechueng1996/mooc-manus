import SpaceActionButton from '../_component/SpaceActionButton';

const DatasetAction = () => {
  return (
    <SpaceActionButton label="Create dataset" href="/space/datasets/create" />
  );
};

export default DatasetAction;
